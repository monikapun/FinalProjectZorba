CREATE TABLE EMPLOYEE
        (
        id NUMBER(10) NOT NULL,
        name VARCHAR2(50) NOT NULL,
        address VARCHAR2(150) NOT NULL,
        phonenumber VARCHAR2(150) NOT NULL,
        salary DOUBLE (10) NOT NULL,
        PRIMARY KEY(id)
        );